# this program checks the password matches the one in the other programs
# its seperate to make it more secret even though its still just as readable
def passcheck(text):
    if text == 16119192315184:
        return True
    else:
        return False